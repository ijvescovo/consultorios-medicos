const express = require('express');
const router = express.Router();

// Placeholder para rutas de turnos
router.get('/', (req, res) => {
    res.json({ message: 'Rutas de turnos - En desarrollo' });
});

module.exports = router;
