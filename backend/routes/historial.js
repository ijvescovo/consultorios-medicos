const express = require('express');
const router = express.Router();

// Placeholder para rutas de historial médico
router.get('/', (req, res) => {
    res.json({ message: 'Rutas de historial médico - En desarrollo' });
});

module.exports = router;
